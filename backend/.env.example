# Green Cast backend environment variables
# Copy to .env and fill in values

# Optional — AI commentary is disabled if absent; rules-based scoring still works
ANTHROPIC_API_KEY=sk-ant-...

# SQLite database path (default: greencast.db in backend directory)
# DATABASE_URL=sqlite:///./greencast.db
